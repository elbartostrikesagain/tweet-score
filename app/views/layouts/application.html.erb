<!DOCTYPE html>
<html>
<head>
  <title>Tweet Score</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
</head>
<body>

  <div data-role="header" class="ui-bar-a ui-header" role="banner">
    <a id="nav-button" href="#" data-icon="gear" class="ui-btn-right">Settings</a>
    <h1 class="ui-title" tabindex="0" role="heading" aria-level="1">Score Tweet</h1>
    <a id="nav-button" href="#" data-icon="gear" class="ui-btn-right">Scores</a>
    <% if current_user %>
      <a href="/sign_out" data-icon="person" class="ui-btn-right ui-btn ui-btn-icon-left ui-btn-corner-all ui-shadow ui-btn-up-a" data-theme="a"><span class="ui-btn-inner ui-btn-corner-all"><span class="ui-btn-text">Logout</span><span class="ui-icon ui-icon-home ui-icon-shadow"></span></span></a>
    <% end %>
  </div>

  <div id="container">
    <div id="user_nav">
      <% if current_user %>
        Signed in as <%= current_user.name %>!
      <% else %>
        <%= link_to "Sign in with Twitter", "/auth/twitter", rel: 'external' %>
      <% end %>
    </div>

    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, id: "flash_#{name}" %>
    <% end %>

    <%= yield %>

    <div class="clear"></div>
  </div>

</body>
</html>